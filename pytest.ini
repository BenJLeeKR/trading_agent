[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = module
testpaths = tests
markers =
    smoke: KIS paper/sandbox read-only smoke test (requires KIS API credentials)
    slow: Time-intensive tests (WebSocket, etc.)
